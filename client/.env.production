# Used only for `npm run build` (i.e. the APK build).
# Replace with your deployed backend's public HTTPS URL — the phone
# cannot reach "localhost", that means the backend running on your PC.
VITE_API_BASE_URL=https://YOUR-DEPLOYED-BACKEND-DOMAIN/api
